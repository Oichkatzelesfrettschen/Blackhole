# v7x86-32 ignored vendor subproject archive

This archive preserves ignored nested Git subproject state removed from the local `v7x86-32` checkout during repository hygiene.

- `ack-current.status.txt`: local Git status for the ignored ACK checkout.
- `ack-current.local-diff.patch`: binary-capable patch for tracked ACK modifications.
- `ack-current-v7x86-untracked-files.txt`: manifest for untracked V7/x86 ACK port files.
- `ack-current-v7x86-untracked-tree.tar.gz`: tarball of untracked `mach/v7x86` and `plat/v7x86` trees.
- `SHA256SUMS`: checksums for archive payloads.

The clean repository already carries the published reconstruction and patch artifacts on `master`; these ignored nested repositories were local scratch/vendor state, not tracked project content.
